services:
  postgres:
    image: postgres:16-alpine
    env_file:
      - path: .env.local
        required: false
      - path: .env
        required: false
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-linkly}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-linkly_local_password}
      POSTGRES_DB: ${POSTGRES_DB:-linkly}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER:-linkly} -d $${POSTGRES_DB:-linkly}"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redisdata:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  db-init:
    image: node:22-bookworm-slim
    working_dir: /workspace
    depends_on:
      postgres:
        condition: service_healthy
    env_file:
      - path: .env.local
        required: false
      - path: .env
        required: false
    environment:
      DATABASE_URL: postgresql://linkly:linkly_local_password@postgres:5432/linkly?schema=public
    volumes:
      - ./:/workspace
      - /workspace/node_modules
      - /workspace/.pnpm-store
    command: >
      bash -c "corepack enable
      && pnpm install --frozen-lockfile
      && pnpm prisma migrate deploy
      && pnpm prisma db seed"
    restart: "no"

volumes:
  pgdata:
  redisdata:
